'use strict';
/*
//set
const orderSet = new Set(['Pizza', 'Pasta', 'Pizza', 'Risotto', 'Pizza']);
console.log(orderSet); //set can avoid duplicate
console.log(new Set('Jonas'));
console.log(new Set());
console.log(orderSet.size);
console.log(orderSet.has('Pizza'));
console.log(orderSet.has('Bread'));
console.log(orderSet.add('Garlic Bread'));
console.log(orderSet.delete('Risotto'));

for (const order of orderSet) {
  console.log(order);
}

//example of removing duplicate
const staff = ['Waiter', 'Chef', 'Waiter', 'Manager', 'Chef', 'Waiter'];
//const staffUnique = new Set(staff); //but what if we want the output be a array
const staffUnique = [...new Set(staff)]; //spread operator work here! convert it into array
console.log(staffUnique);
console.log(
  new Set(['Waiter', 'Chef', 'Waiter', 'Manager', 'Chef', 'Waiter']).size
);
*/
